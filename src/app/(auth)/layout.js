export const metadata = {
    title: "LogIn - Equilis - Beyond connectivity, towards intelligence",
    description: "Equilis - Beyond connectivity, towards intelligence",
  };

export default function AuthLayout({ children }) {
    return (
      <>
       {children}
      </>
    );
  }
  